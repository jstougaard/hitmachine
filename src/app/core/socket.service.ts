/// <reference path="../../types/types.ts"/>

/*interface ISocketConnectionService {
    sendData():void;
}

class Socket implements ISocketConnectionService {
    static datastream: ng.socketIO.IWebSocket;

    *//* @ngInject *//*
    constructor(private socketFactory: any) {
        console.log("Socket constructed!", socketFactory);
        if (!Socket.datastream) {
            Socket.datastream = <ng.socketIO.IWebSocket>socketFactory();
        }
    }

    sendData() {
        return "SOME DATA";
    }
}

angular.module('core').service('Socket', Socket);*/
